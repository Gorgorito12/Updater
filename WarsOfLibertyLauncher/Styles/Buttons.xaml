<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ================================================================
         IMPLICIT BUTTON STYLE — global Aero replacement.

         WPF's built-in Button template paints a sky-blue gradient on
         hover that screams "Windows 7" against the launcher's dark
         imperial chrome. Any Button defined without an explicit
         Style="{StaticResource ...}" inherits that default, which is
         why the Workshop CTAs, filter chips, and Multiplayer sub-
         tabs kept "going white" in hover no matter how many
         individual styles we patched.

         Fix: this implicit Style (no x:Key) targets every Button
         that doesn't pick an explicit named Style. It uses the
         OVERLAY PATTERN — a low-alpha white scrim layered on top of
         the button's own Background, instead of replacing the
         background on hover. That way:

           • A CatalogBlue CTA stays blue on hover (just brighter).
           • A BgPanelAlt secondary stays its panel grey (just brighter).
           • A transparent ghost still reads its parent surface.

         Steam, Discord and Material Design all use this overlay
         technique for exactly this reason — it sidesteps every
         "wrong colour on hover" bug because the base colour never
         changes.

         Named styles that already define their own Template
         (NavTabButton, MpSecondaryButton, PropertyActionButton, etc.)
         are NOT affected by this — their Template setter wins
         locally. The implicit only catches buttons that fall through
         to the default. Named styles WITHOUT a Template (FilterChip
         is the only remaining one) need BasedOn="{StaticResource
         {x:Type Button}}" to opt back into this implicit.
         ================================================================ -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource RadiusSm}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <!-- Overlay scrim. IsHitTestVisible=False so it
                             never steals clicks. CornerRadius matches
                             the base border so the brighten clips
                             cleanly to the button's rounded edge. -->
                        <Border x:Name="overlay"
                                Background="White"
                                Opacity="0"
                                CornerRadius="{StaticResource RadiusSm}"
                                IsHitTestVisible="False"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!-- 8% white scrim. Looks subtle on a
                                 transparent button (just brightens
                                 the parent surface a touch) and
                                 distinctly "lifted" on a coloured CTA. -->
                            <Setter TargetName="overlay" Property="Opacity" Value="0.08"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <!-- 14% — the press should be clearly
                                 stronger than the hover so the click
                                 feedback is unambiguous. -->
                            <Setter TargetName="overlay" Property="Opacity" Value="0.14"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <!-- Colour, never an Opacity layer: `border` wraps the
                                 ContentPresenter, so dimming it composites the caption and
                                 costs it ClearType. See TextDisabled in Colors.xaml. -->
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================================
         PropertyActionButton — SECONDARY GOLD recipe for the
         per-mod Properties dialog. Every action button on that
         dialog (Open folder, Open AoE3 folder, Verify files,
         Repair install, View logs, Open user data folder,
         Create backup, Restore backup, Change mod folder,
         Change AoE3 folder) shares the imperial dorado identity
         of the active mod chrome — pure gris-neutral DialogButton
         hovers were too "system dialog" for a per-mod surface.

         Visual contract:
           Idle:    transparent bg + subtle BorderSecondary outline
                    + TextPrimary text
           Hover:   12% gold tile fill (TintGoldHover) + AccentBrush
                    gold border + warm cream (FgHoverGold) text
           Disabled: BgPanelAlt fill + #666 text

         Uninstall sits outside this style — it stays DangerButton
         (red) because destructive actions never share their hover
         treatment with safe ones. -->
    <Style x:Key="PropertyActionButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSecondary}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="{StaticResource BtnPadRow}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusSm}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource TintGoldHover}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource FgHoverGold}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource BgPanelAlt}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BorderSubtle}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Neutral dialog button. Base for Primary / Danger / Success. -->
    <Style x:Key="DialogButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource BgNeutral}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="{StaticResource BtnPadRoomy}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource RadiusSm}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- No Background here, and it is load-bearing. This template is
                             INHERITED (PrimaryButton and DangerButton are BasedOn this
                             style), and a ControlTemplate trigger that paints a template
                             element BY NAME cannot be overridden by a derived style — a
                             Style trigger can only set the BUTTON's property, which the
                             TargetName setter then beats on the way down. It used to set
                             border.Background here, so PrimaryButton's gold hover and
                             DangerButton's red one were declared, inherited, and silently
                             DEAD: every confirm and every destructive button in ~15
                             dialogs turned neutral grey on hover instead. Background
                             states now live in each Style's own triggers below, where a
                             derived style can replace them. The implicit Button style at
                             the top of this file solves the same problem the other way,
                             by animating an overlay's Opacity instead of a colour. -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <!-- The neutral dialog button's own states. Same colours as before; they just
                 travel through the Button's Background and the TemplateBinding now. -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource BgNeutralHover}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource BgPanelAlt}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Accent-colored confirm. DynamicResource so the per-mod theme swap
         in MainWindow propagates to every dialog that's already open.
         Its hover/disabled triggers below come AFTER DialogButton's in the merged
         BasedOn chain, so they are the ones that land. -->
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
        <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
        <!-- DialogButton's White is unreadable here: #FFFFFF on AccentBrush #D8B66A is
             1.94:1, and 1.65:1 against AccentBrushHover. Gold is a LIGHT fill and wants dark
             text — which SidebarPrimaryButton (PLAY) already does on the same gold with
             #261900, at 8.86:1. Disabled still comes from DialogButton's template trigger. -->
        <Setter Property="Foreground" Value="#261900"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource AccentBrushHover}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource BgNeutral}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Destructive confirm (Uninstall). Never themed per mod. -->
    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
        <Setter Property="Background" Value="{DynamicResource DangerBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource DangerBrushHover}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource BgNeutral}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- MainWindow sidebar: the big PLAY / INSTALL button.
         Fase 3 redesign: vertical gold gradient + warm drop shadow +
         subtle inner highlight, matching the Stitch hero CTA. Uses
         the display serif (DisplayFont) so the primary CTA reads as
         premium / cinematic. Background is set inside the
         ControlTemplate (a LinearGradientBrush) instead of via the
         Background setter, so per-button overrides don't accidentally
         flatten the gradient back to a solid colour. -->
    <Style x:Key="SidebarPrimaryButton" TargetType="Button">
        <Setter Property="Foreground" Value="#261900"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontFamily" Value="{DynamicResource DisplayFont}"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeSubtitle}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="{StaticResource BtnPadCta}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                  <!-- Two layers, and the split is the point: the glow lives on `halo`,
                       UNDER the button, never on `border`. `border` wraps the
                       ContentPresenter, and an Effect on an ancestor of text pushes the whole
                       subtree through a composition pass — ClearType off for the PLAY caption,
                       permanently, on the largest button in the launcher. `halo` is fully
                       covered by `border` (same size, same radius), so only its shadow shows.
                       Same pattern as the multiplayer cards and MpAlertOverlay. -->
                  <Grid>
                    <Border x:Name="halo"
                            CornerRadius="{StaticResource RadiusMd}"
                            Background="{DynamicResource AccentBrush}"
                            IsHitTestVisible="False">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="14"
                                              ShadowDepth="2"
                                              Color="{Binding Source={StaticResource AccentBrush}, Path=Color}"
                                              Opacity="0.55"/>
                        </Border.Effect>
                    </Border>
                    <Border x:Name="border"
                            CornerRadius="{StaticResource RadiusMd}"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource AccentBrushHover}"
                            Padding="{TemplateBinding Padding}">
                        <Border.Background>
                            <!-- Top → bottom: AccentBrushHover (lighter cream
                                 gold) → AccentBrush (base) → AccentDeep
                                 (richer ochre). Reads as a "lit metal" PLAY
                                 button. All stops are tokens so retheming
                                 the dorado in Colors.xaml propagates here. -->
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="{Binding Source={StaticResource AccentBrushHover}, Path=Color}" Offset="0.0"/>
                                <GradientStop Color="{Binding Source={StaticResource AccentBrush}, Path=Color}" Offset="0.5"/>
                                <GradientStop Color="{Binding Source={StaticResource AccentDeep}, Path=Color}" Offset="1.0"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                  </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background">
                                <Setter.Value>
                                    <!-- Hover: shift every stop slightly
                                         lighter so the gradient "rises"
                                         under the cursor. Still bound to
                                         the dorado tokens so retheming
                                         hits both states. -->
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="{Binding Source={StaticResource FgHoverGold}, Path=Color}" Offset="0.0"/>
                                        <GradientStop Color="{Binding Source={StaticResource AccentBrushHover}, Path=Color}" Offset="0.5"/>
                                        <GradientStop Color="{Binding Source={StaticResource AccentBrush}, Path=Color}" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="halo" Property="Effect">
                                <Setter.Value>
                                    <!-- Pressed: kill the outer glow so the
                                         button reads as "depressed". -->
                                    <DropShadowEffect BlurRadius="4"
                                                      ShadowDepth="1"
                                                      Color="Black"
                                                      Opacity="0.4"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource BgNeutral}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BorderSubtle}"/>
                            <Setter TargetName="halo" Property="Effect" Value="{x:Null}"/>
                            <Setter Property="Foreground" Value="#888"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Dorado progress bar (Fase 3). Replaces the default WPF
         ProgressBar's flat slab with a gold gradient fill on a dark
         groove. Originally also had an animated shimmer overlay but
         the EventTrigger / Storyboard inside a ControlTemplate threw
         InvalidOperationException "name 'Shimmer' not found in scope"
         at runtime (WPF's template namescope doesn't expose named
         elements to Storyboard.TargetName the way it does to Trigger
         setters). Static gradient now; if we want shimmer later it has
         to be done via code-behind subscribing to Loaded and starting
         a Storyboard programmatically.
         Applied via Style="{StaticResource ShimmerProgressBar}" on
         each ProgressBar that should have it. Dialog progress bars
         (LauncherUpdateDialog, TranslationApplyDialog) keep the WPF
         default by not setting Style — opt-in. -->
    <Style x:Key="ShimmerProgressBar" TargetType="ProgressBar">
        <!-- Foreground holds the indicator gradient; the template
             binds the PART_Indicator's Background to it so callers
             can swap per-operation tones (install / update / repair /
             uninstall) just by reassigning Foreground. The default
             is the dorado dorado gradient that matches the rest of
             the imperial chrome. -->
        <Setter Property="Foreground">
            <Setter.Value>
                <!-- Default progress gradient = dorado, same stops as
                     ProgressGradientUpdate in Colors.xaml. Bound to
                     the Modern Colonial dorado tokens so retheming
                     the palette propagates here too. -->
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="{Binding Source={StaticResource AccentDeep}, Path=Color}" Offset="0.0"/>
                    <GradientStop Color="{Binding Source={StaticResource AccentBrushHover}, Path=Color}" Offset="0.5"/>
                    <GradientStop Color="{Binding Source={StaticResource AccentBrush}, Path=Color}" Offset="1.0"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Background" Value="{DynamicResource BgPanel}"/>
        <Setter Property="Height" Value="6"/>
        <Setter Property="MinHeight" Value="4"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <!-- BOTH children must be named with their PART_*
                         identifiers. WPF's internal
                         SetProgressBarIndicatorLength logic does
                         `_indicator.Width = (Value/Max) * _track.ActualWidth`
                         and bails out if either lookup returns null. A
                         previous version of this template only named
                         PART_Indicator, so _track was null, the width
                         never got set, and the bar visually froze at
                         0% across every operation. -->
                    <Grid>
                        <Border x:Name="PART_Track"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3"/>
                        <Border x:Name="PART_Indicator"
                                HorizontalAlignment="Left"
                                CornerRadius="3"
                                Background="{TemplateBinding Foreground}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Sidebar action buttons — Background/BorderBrush set per instance so
         one style serves Update (blue), Verify (purple), Repair (amber),
         Uninstall (red), etc. -->
    <Style x:Key="SidebarColoredButton" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="{StaticResource BtnPadDefault}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusSm}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SidebarSecondaryButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource BgPanelAlt}"/>
        <Setter Property="Foreground" Value="#ddd"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Padding" Value="{StaticResource BtnPadDefault}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource RadiusSm}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource BgNeutral}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource BgPanel}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================================
         Sidebar navigation buttons (Fase 2 redesign). Vertical stack
         in the left rail. Each item is icon + label. Active state
         signalled via Tag="active" set programmatically by
         RefreshSidebarHighlight() — keeps the visual contract in one
         place instead of toggling Background per click.
         ============================================================ -->
    <Style x:Key="SidebarNavButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <!-- Inactive label/icon: cool blue-gray (OnSecondaryContainer
             #a9bad0). Provides the cool secondary tone the HTML mock
             uses for sleeping nav items so the dorado active state
             reads warm by contrast. Active state is forced to
             AccentBrush (gold) via the Tag="active" trigger. -->
        <Setter Property="Foreground" Value="{DynamicResource OnSecondaryContainer}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <!-- Padding bumped 14→16 vertically so the bigger 15pt label
             doesn't crowd the row edges. Horizontal stays 22 — the
             dorado right-rail accent on the active state needs the
             same left/right breathing room it had before. -->
        <Setter Property="Padding" Value="22,16"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="FontFamily" Value="{DynamicResource DisplayFont}"/>
        <!-- 13 → 15: noticeably bigger nav labels without making the
             240px sidebar feel cramped. "MULTIJUGADOR" (longest
             label in either language at 12 chars) still has ~50px
             of headroom inside the panel. -->
        <Setter Property="FontSize" Value="{DynamicResource FontSizeSubtitle}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="0,0,2,0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!-- No background fill — keep the rail
                                 visually quiet. Text lifts to near-
                                 white (#E6EEF8) so the hover whispers
                                 a state change without painting a tile. -->
                            <Setter Property="Foreground" Value="#E6EEF8"/>
                        </Trigger>
                        <Trigger Property="Tag" Value="active">
                            <!-- Active: pure white text + bold + gold right-rail
                                 stripe (the original look). The rail column is
                                 Width="Auto", so the bold active item can widen
                                 the rail by a few px when the LONGEST label is
                                 selected — an accepted minor trade-off for
                                 keeping the original bold emphasis. -->
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Text label inside a SidebarNavButton (the settings/properties rail).
         Centralised so the rail's labels are consistent and — crucially for
         the Width="Auto" rail clamped to SidebarRailMaxWidth — ellipsise
         instead of clipping silently when a label is longer than the max.
         The icon glyph next to it keeps its own inline size. -->
    <!-- BasedOn the implicit TextBlock style (Chrome.xaml) is load-bearing, not
         tidiness: a NAMED style does not inherit the implicit one, and this is the
         only named TextBlock style in the repo that sets TextTrimming — so without
         it the rail's labels would be the one place in the launcher where a cut
         label could not be read. -->
    <Style x:Key="SidebarNavLabel" TargetType="TextBlock"
           BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="FontSize" Value="{DynamicResource SidebarNavTextSize}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <!-- Cinema-dashboard secondary action button. Used for the gear
         (mod actions menu) and the "Change mod" pill that sit next
         to the gold hero PLAY button. Visual goal: present but
         quiet — translucent dark surface, cool blue-gray border &
         icon at rest, dorado warm-up on hover. The two-tone hover
         transition (cool → gold) is what gives the hero row the
         secondary↔primary visual hierarchy the HTML mock has. -->
    <Style x:Key="DashboardSecondaryButton" TargetType="Button">
        <Setter Property="Background" Value="#33000000"/>
        <Setter Property="Foreground" Value="{DynamicResource Secondary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSecondary}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="{StaticResource BtnPadRow}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontFamily" Value="{DynamicResource DisplayFont}"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusMd}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!-- SECONDARY GOLD recipe: low-alpha gold
                                 tile fill + gold border + warm cream
                                 text. The tile fill makes the button
                                 feel "lit" instead of flat — matches
                                 the imperial paleta the dashboard
                                 sits on. Cream (#FFF4E0) instead of
                                 pure white reads warmer against the
                                 gold tint. -->
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource TintGoldHover}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource FgHoverGold}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#747E90"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#2a323d"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================================
         Horizontal nav tabs (Steam-style redesign). Replaces the
         vertical SidebarNavButton when the launcher chrome is laid
         out as a horizontal strip below the title bar. Visual
         contract:
           - Inactive: cool secondary text/icon, transparent bg
           - Hover:    8% dorado tint bg, dorado text/icon
           - Active:   12% dorado tint bg, 3px bottom underline in
                       dorado, dorado text/icon, bold weight
         The active state is driven by Tag="active" — same pattern
         RefreshTopTabHighlight uses for SidebarNavButton, so the
         code-behind doesn't need to change.

         Earlier revision used CatalogBlue for hover/active to align
         with the slate panel; reverted to dorado per user request
         because the gold interaction state matches the rest of the
         imperial chrome (PLAY button, brand wordmark, hero title).
         The panel BACKGROUND stays slate (BgSidebar #33414F) — only
         the per-button hover/active are gold.
         ============================================================ -->
    <!-- (LangPillButton, the ES/EN segmented pill, used to sit here. The header
         reference has no language control — it is changed in Launcher Settings, which
         was always the primary place — so the style went with the markup rather than
         staying declared for a control nothing builds.) -->

    <!-- REDESIGN NOTE: the active marker is a 2px underline on the button's bottom
         edge — and whether that is right depends entirely on where the tabs live.
         Originally an underline; changed to a filled pill by handoff 1a, which moved
         the tabs INTO the title bar, where there is no chrome/content seam for a line
         to sit on and an underline just drew through the middle of the chrome; back to
         an underline now that the header reference gives the tabs a row of their own
         with a rule under it. The line lands ON that rule, which is the whole point.
         If the tabs ever move again, this marker moves with them. -->
    <Style x:Key="NavTabButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource ChromeTextDim}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <!-- 16 horizontal from the reference; no vertical padding because the button
             stretches to the row height and the label is centred in it. -->
        <Setter Property="Padding" Value="16,0"/>
        <Setter Property="Margin" Value="4,0,0,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <!-- Stretch, so the button fills the nav row's full height — which is what lets
             the underline sit on the row's bottom edge rather than floating under the
             text. (It also used to matter for a second reason, back when these lived in
             the caption region: any pixel not covered by a control dragged the window.
             The nav row is ordinary client area, so that hazard is gone.) -->
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontFamily" Value="{DynamicResource BodyFont}"/>
        <Setter Property="FontSize" Value="{DynamicResource NavTabTextSize}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <!-- The 2px underline is reserved as TRANSPARENT at rest rather than
                         added when active: a 0→2 flip would shift the label 2px on every
                         tab switch. Same class of jitter as re-weighting the text, which
                         is why the active tab is deliberately not bold either. -->
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="0,0,0,2"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!-- Hover stays quiet — text only, no fill — so the underline
                                 remains the unambiguous "you are here". -->
                            <Setter Property="Foreground" Value="{DynamicResource ChromeTextSoft}"/>
                        </Trigger>
                        <Trigger Property="Tag" Value="active">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource MpAction}"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- (NavBrandButton style removed — was only used for the
         "AOE3 LAUNCHER ▼" dropdown in the horizontal nav strip,
         which itself was removed once the title bar's
         "AoE3 Mod Launcher" button took over the same job. The
         in-title-bar button uses an inline ControlTemplate rather
         than a reusable style.) -->

    <!-- Secondary sidebar links (Support / Wiki at the bottom of the
         rail). Lighter weight, smaller, no active state since they
         open external links instead of switching the content view. -->
    <Style x:Key="SidebarFooterButton" TargetType="Button" BasedOn="{StaticResource SidebarNavButton}">
        <Setter Property="Padding" Value="22,8"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeCaption}"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <!-- Slightly dimmer cool tone for Support/Wiki — they're not
             primary navigation so they recede a step. Matches the
             OnSecondaryFixedVariant tone (#38485a) lightened a touch
             to stay legible against the BgSidebar surface. -->
        <Setter Property="Foreground" Value="#788aa0"/>
    </Style>

    <!-- App-level top tabs (Play / Mods / Multiplayer / Settings).
         Kept for backward compat — nothing references it after Fase 2
         but harmless to leave. Will be deleted in a future cleanup
         pass once the codebase is fully on the sidebar layout. -->
    <Style x:Key="TopTabButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#888"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontFamily" Value="{DynamicResource DisplayFont}"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeSubtitle}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="0,16"/>
        <Setter Property="Margin" Value="0,0,28,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            BorderThickness="0,0,0,3"
                            BorderBrush="Transparent"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Left"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Right content panel tabs (Noticias / Changelog / Ayuda). Active
         underline is set programmatically by RefreshTabsHighlight(). -->
    <Style x:Key="TabButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#888"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="0,10"/>
        <Setter Property="Margin" Value="0,0,28,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            BorderThickness="0,0,0,2"
                            BorderBrush="Transparent"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Left"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- =========================================================
         Multiplayer-tab button styles. Kept separate from the
         per-mod accent styles above so the multiplayer surface can
         keep a stable blue identity regardless of which mod is
         active (matches the redesign brief — red is reserved for
         destructive / error states only).
         ========================================================= -->

    <!-- Primary blue CTA: Create room, Join, Ready, Send, etc. -->
    <Style x:Key="MpPrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource MpBlue}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="{StaticResource BtnPadDefault}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource RadiusSm}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource MpBlueHover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource MpBluePressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource BgNeutral}"/>
                            <Setter Property="Foreground" Value="#888"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Secondary neutral button: Refresh, Cancel, Join (row) etc. -->
    <Style x:Key="MpSecondaryButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource MpSurfaceAlt}"/>
        <Setter Property="Foreground" Value="#dadde2"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtle}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="{StaticResource BtnPadDefault}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusSm}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!-- SECONDARY BLUE recipe: BgNeutral fill
                                 + white text. Restored to the original
                                 multiplayer-paleta hover after the
                                 global "blank everywhere" pass over-
                                 reached and washed the section out. -->
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource BgNeutral}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource BgPanel}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Outline blue (empty-state Create button under the illustration). -->
    <!-- Room-row action buttons (design handoff 1a). The reference gives the two
         ENABLED states different weights on purpose: joining a room you are not in is
         the table's primary act and gets a solid fill, while returning to the room you
         are already in is a way back rather than an invitation, so it is a ghost. They
         used to share one outline style, which made every row look equally like a call
         to action. Disabled states keep the neutral MpSecondaryButton. -->
    <Style x:Key="MpRoomActionPrimary" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource MpAction}"/>
        <Setter Property="Foreground" Value="#FFFFFF"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Height" Value="{StaticResource MpControlHeight}"/>
        <Setter Property="Padding" Value="14,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="b"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource RadiusControl}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="b" Property="Background" Value="{DynamicResource MpBlueHover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MpRoomActionGhost" TargetType="Button" BasedOn="{StaticResource MpRoomActionPrimary}">
        <Setter Property="Background" Value="{DynamicResource MpActionSoftBg}"/>
        <Setter Property="Foreground" Value="{DynamicResource MpActionText}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource MpActionRim}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource RadiusControl}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MpOutlineBlueButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#7fb0ff"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MpBlue}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="{StaticResource BtnPadDefault}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusSm}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!-- SECONDARY BLUE outline recipe:
                                 MpBlueSubtle fill (low-alpha blue) +
                                 white text. The MpBlue outline stays
                                 (it's the identity of this button). -->
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource MpBlueSubtle}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- GOLD outline (room-card "Unirse"/"Reingresar"). Same shape as
         MpOutlineBlueButton but in the launcher's dorado accent — used on the
         room cards to match the reference mockup. Disabled state dims. -->
    <Style x:Key="MpOutlineGoldButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="{StaticResource BtnPadDefault}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusSm}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#22D8B66A"/>
                            <Setter Property="Foreground" Value="#F0DCA8"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <!-- Colour, never an Opacity layer: `border` wraps the
                                 ContentPresenter, so dimming it composites the caption and
                                 costs it ClearType. See TextDisabled in Colors.xaml. -->
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Destructive (Leave room). Darker red than DangerBrush. -->
    <Style x:Key="MpDangerButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource MpDanger}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="{StaticResource BtnPadDefault}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource RadiusSm}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource MpDangerHover}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource BgNeutral}"/>
                            <Setter Property="Foreground" Value="#888"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Transparent square icon button (favorite star, close X, etc).
         Foreground gets coloured per-instance for the favorite-filled
         vs favorite-empty state. -->
    <Style x:Key="MpIconButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Width" Value="28"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource RadiusSm}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!-- GHOST BLUE recipe: tint surface
                                 (MpSurfaceAlt) so the glyph reads as
                                 a hit-target square. Restored after
                                 the global pass blanked the fill. -->
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource MpSurfaceAlt}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Ready button (room sidebar). Looks like a "checkbox in button
         form": green-outlined and transparent when idle, SOLID GREEN
         when active. Toggled by the code-behind via Tag="ready" /
         Tag="" (RenderRoomPanel) so we keep one visual style and one
         click handler.

         The Tag="ready" trigger is the ONLY thing that paints the
         active state — it was missing for a long time, so the button
         set its Tag, swapped its ✓/○ label, and never turned green
         ("el botón grande de marcar no se pone verde"). If the green
         ever disappears again, suspect this trigger, not the
         code-behind.

         Trigger ORDER is load-bearing: for a given property the LAST
         active trigger wins, so Tag="ready" must come AFTER IsMouseOver
         (else hovering a ready button would wash the green away), the
         ready+hover MultiTrigger after both (so a ready button still
         reacts to the mouse instead of feeling dead), and IsEnabled
         last so disabled always overrides. This works without a local
         Background/Foreground on the button (LobbyWindow.xaml sets
         neither) — a local value would beat every trigger here. -->
    <Style x:Key="MpReadyButton" TargetType="Button">
        <!-- Ghost at rest, to match its Leave sibling: the two secondary actions carry
             the same weight, and the green belongs to the READY state, not to the button
             that offers it. Still no local Background/Foreground on the instance — a
             local value beats every trigger below, which is what once left this button
             stuck grey while the roster row already showed the player as ready. -->
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource MpTextSecondary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MpRimStrong}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Height" Value="{StaticResource MpFieldHeight}"/>
        <Setter Property="FontSize" Value="{DynamicResource MpMetaSize}"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Padding" Value="10,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusControl}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource MpRowHighlight}"/>
                        </Trigger>
                        <Trigger Property="Tag" Value="ready">
                            <Setter TargetName="border" Property="Background" Value="#15803D"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource MpStatusReady}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Tag" Value="ready"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="border" Property="Background" Value="#16A34A"/>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BgNeutral}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Compact secondary used for "Clear chat" in the room view
         header. Smaller padding + lighter weight than the global
         MpSecondaryButton so it sits as a quiet header utility,
         not a primary action. -->
    <Style x:Key="MpQuietButton" TargetType="Button" BasedOn="{StaticResource MpSecondaryButton}">
        <Setter Property="Padding" Value="{StaticResource BtnPadCompact}"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeCaption}"/>
        <Setter Property="FontWeight" Value="Medium"/>
    </Style>

    <!-- Ghost buttons for the lobby actions (design handoff 1e). The room used to show
         three SOLID buttons — green Ready, blue Start, red Leave — competing for the same
         attention, when only one of them is what the player came to do. The primary keeps
         its fill; these two carry a rim and nothing else. -->
    <Style x:Key="MpGhostButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource MpTextSecondary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MpRimStrong}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Height" Value="{StaticResource MpFieldHeight}"/>
        <Setter Property="Padding" Value="10,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="{DynamicResource MpMetaSize}"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="b"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusControl}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="b" Property="Background" Value="{DynamicResource MpRowHighlight}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MpGhostDangerButton" TargetType="Button" BasedOn="{StaticResource MpGhostButton}">
        <Setter Property="Foreground" Value="{DynamicResource MpDestructiveText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MpDestructiveRim}"/>
    </Style>

    <Style x:Key="MpGhostCautionButton" TargetType="Button" BasedOn="{StaticResource MpGhostButton}">
        <Setter Property="Foreground" Value="{DynamicResource MpCautionTextAlt}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MpCautionRimBox}"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="FontSize" Value="{DynamicResource MpLabelSize}"/>
    </Style>

    <!-- Text link (design handoff 1a). The reference renders the "join" affordance
         on the chat's room-opened card as plain coloured text at the end of the row,
         not as a bordered button: inside a chat flow a real button reads as heavier
         than the message it belongs to. Still a Button, so the click, the keyboard
         focus and the disabled state are the framework's. -->
    <!-- A whole ROW that happens to be clickable: the fold header of a civilization group in
         the community-cards table. No chrome of its own, stretched content, and a hover that
         only lifts the background a shade - the row's own text and figures carry the look, and
         a button frame around them would read as a second control sitting inside the table.

         A Button rather than a Border with a mouse handler so it is reachable by keyboard and
         announces itself, and so the hit target is the entire header instead of the four-pixel
         caret at its left edge. -->
    <Style x:Key="MpBareButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Focusable" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="face" Background="Transparent">
                        <ContentPresenter HorizontalAlignment="Stretch"
                                          VerticalAlignment="Stretch"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="face" Property="Background"
                                    Value="{DynamicResource MpRowHighlight}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="face" Property="Background"
                                    Value="{DynamicResource MpRowHighlight}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MpLinkButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource MpActionText}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="{DynamicResource MpLabelSize}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                        <ContentPresenter x:Name="content"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="content" Property="TextBlock.TextDecorations" Value="Underline"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- The same link on the launcher's gold chrome. Same shape, same underline on hover,
         the palette of the window it is in — the sign-in dialog is BgBase and AccentBrush,
         not the multiplayer navy, and its privacy link is already this colour.

         Derived rather than set per instance, for the precedence reason spelled out under
         MpNoteLinkButton below: a local Foreground outranks the base template's
         IsEnabled=False trigger and a disabled link would stay lit. -->
    <Style x:Key="ChromeLinkButton" TargetType="Button" BasedOn="{StaticResource MpLinkButton}">
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
    </Style>

    <!-- The link inside an amber note — "See how", on a history card that did not count. It
         wears the note's own colour so it reads as part of the explanation rather than as a
         second, unrelated action.

         A derived STYLE and not a local Foreground on the instance, which is the precedence
         trap this file documents twice: a local value (3) beats the base template's
         IsEnabled=False trigger (7), so a locally-coloured link would keep its colour while
         disabled. A Style setter is 8, below that trigger, so the disabled state still wins. -->
    <Style x:Key="MpNoteLinkButton" TargetType="Button" BasedOn="{StaticResource MpLinkButton}">
        <Setter Property="Foreground" Value="{DynamicResource MpProvisionalText}"/>
    </Style>

    <!-- Row-level Join button: the small one inside each room row in
         the rooms table. Looks like Secondary but with hover that
         shifts to MpBlue so it reads as "the primary action of this
         row" without competing with the global Create room button. -->
    <Style x:Key="MpRowJoinButton" TargetType="Button" BasedOn="{StaticResource MpSecondaryButton}">
        <Setter Property="MinWidth" Value="74"/>
        <Setter Property="Padding" Value="14,5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusSm}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource MpBlue}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource MpBlue}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource BgPanel}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================================
         Segmented control: one row of mutually exclusive choices, the
         chosen one filled. For SHORT excluding options only - a format
         (1v1/2v2/3v3), a capacity (2/4/8/16), a who-may-enter. Anything
         needing a line of explanation per option stays a RadioButton.

         Lives here rather than in a dialog because three surfaces use it:
         the room dialog's max-players row it was written for, the new
         tournament dialog, and the room-size row beside it. Declared in
         CreateLobbyDialog.xaml it was reachable from nowhere else, and
         MultiplayerTab.xaml still carries the comment that said so.

         The active state is set through Tag ("active"), so a row can be
         rebuilt from code without touching a brush.
         ============================================================ -->
    <!-- One segment of the max-players row. The active one is a solid blue fill;
         the rest are a rim only. Set through Tag, so the row can be rebuilt from
         code without touching brushes. -->
    <Style x:Key="MpSegment" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource MpTextBody}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MpRimMedium}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="0,0,4,0"/>
        <Setter Property="Padding" Value="0,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="{DynamicResource MpMetaSize}"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="seg"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="seg" Property="Background" Value="{DynamicResource MpRowHighlight}"/>
                        </Trigger>
                        <Trigger Property="Tag" Value="active">
                            <Setter TargetName="seg" Property="Background" Value="{DynamicResource MpAction}"/>
                            <Setter TargetName="seg" Property="BorderBrush" Value="{DynamicResource MpAction}"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================================
         Icon-disc styles (circular icon/avatar holders). Size + radius
         are paired from the App.xaml geometry tokens so the radius can
         never drift from size/2 (a manual Width=36 / CornerRadius=18 is
         easy to get wrong on edit). Apply ONLY to CIRCULAR discs —
         rounded SQUARES (e.g. the 64px dashboard hero icon host) use a
         Radius* token directly instead. -->
    <Style x:Key="IconDiscSm" TargetType="Border">
        <Setter Property="Width" Value="{StaticResource DiscSizeSm}"/>
        <Setter Property="Height" Value="{StaticResource DiscSizeSm}"/>
        <Setter Property="CornerRadius" Value="{StaticResource DiscRadiusSm}"/>
    </Style>
    <Style x:Key="IconDiscMd" TargetType="Border">
        <Setter Property="Width" Value="{StaticResource DiscSizeMd}"/>
        <Setter Property="Height" Value="{StaticResource DiscSizeMd}"/>
        <Setter Property="CornerRadius" Value="{StaticResource DiscRadiusMd}"/>
    </Style>
    <Style x:Key="IconDiscXxl" TargetType="Border">
        <Setter Property="Width" Value="{StaticResource DiscSizeXxl}"/>
        <Setter Property="Height" Value="{StaticResource DiscSizeXxl}"/>
        <Setter Property="CornerRadius" Value="{StaticResource DiscRadiusXxl}"/>
    </Style>

    <!-- ============================================================
         NOTIFICATION BELL PANEL — header link buttons + list rows.
         Used by the Steam-style notification popup in MainWindow.
         ============================================================ -->

    <!-- Small text "link" button for the panel header (Mark all read / Clear). -->
    <Style x:Key="NotifLinkButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Notification BELL button (title bar). Steam-style: a filled, rounded,
         inset tile so it reads as a clear hit-target instead of a faint glyph.
         Gold tile = both the launcher's accent AND a "notifications/attention"
         colour; dark glyph for max contrast. Hover lifts to the brighter gold,
         press to the deep gold. The red unread badge (in MainWindow) sits on
         top of this tile. -->
    <Style x:Key="NotificationBellButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="48"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <!-- Transparent at rest; fills only on hover, press, or while the
                         panel is open (Tag=open). It used to be a filled #283844 tile
                         with rounded corners, which read as a lighter block sitting
                         next to the caption buttons — more so against the near-black
                         title bar the header reference asks for. Squared off and
                         full-height so it behaves like the min/max/close beside it, and
                         sharing their hover so that corner of the bar is one strip.
                         The bell (a Path in the content) is translucent white with a
                         thin black outline. -->
                    <Border x:Name="border"
                            Background="Transparent"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource TitleBarButtonHoverBg}"/>
                        </Trigger>
                        <Trigger Property="Tag" Value="open">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource TitleBarButtonPressedBg}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource TitleBarButtonPressedBg}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Clean tooltip for the notification bell (the default WPF tooltip
         inherits the theme's light Foreground over a pale background → an
         unreadable "black box"). Dark readable card, rounded, small. -->
    <Style x:Key="NotifToolTipStyle" TargetType="ToolTip">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="HasDropShadow" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{DynamicResource BgBase}"
                            BorderBrush="{DynamicResource MpDivider}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="8,4">
                        <ContentPresenter TextElement.Foreground="{DynamicResource TextPrimary}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Full-width clickable notification row: transparent idle, subtle tint
         on hover (so the whole row reads as a hit-target). -->
    <Style x:Key="NotifRowButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Stretch"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource MpSurfaceAlt}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        Community-link pill, used by every mod's Workshop detail panel and by any
        link type added later — the point of it being a keyed Style rather than
        properties set per button.

        It replaced a version that set Background and BorderBrush as LOCAL values
        on each button, which silently killed every hover trigger: a local value
        (precedence 3) beats ControlTemplate.Triggers (4-6), so the pills never
        reacted to the mouse and read as static badges. Same trap documented for
        TitleBarBrandButton. Set colours HERE, never on the instance.

        The label TextBlock inside must NOT set its own Foreground: the
        ContentPresenter propagates the button's Foreground to the content, which
        is the only way the hover trigger reaches the caption. The leading glyph
        DOES set one (gold), deliberately, so it stays gold on hover.
    -->
    <Style x:Key="ModLinkPill" TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="Background" Value="{DynamicResource BgBase}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtle}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="PillBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusMd}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PillBorder" Property="Background"
                                    Value="{DynamicResource TintGoldHover}"/>
                            <Setter TargetName="PillBorder" Property="BorderBrush"
                                    Value="{DynamicResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource SecondaryFixed}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PillBorder" Property="Background"
                                    Value="{DynamicResource TintGoldHoverSubtle}"/>
                            <Setter TargetName="PillBorder" Property="BorderBrush"
                                    Value="{DynamicResource AccentDeep}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
